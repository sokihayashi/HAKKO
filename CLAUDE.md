# CLAUDE.md — HAKKO プロジェクト共有メモ

このファイルは Claude Code がプロジェクトを開いたとき自動で読み込む前提のメモ。

## 前提
このプロジェクトは iOS アプリ「HAKKO（発光）」。
同フォルダの `HAKKO_仕様書.md` が**唯一の仕様**。実装前に必ず仕様書の「1. 設計原則」を読み、それに反する提案はしないこと。
段階的な実装手順は `HAKKO_段階プロンプト集.md` に従う。

## 技術スタック
Swift + SwiftUI + AVFoundation + Core Haptics + Core Image / Metal。
完全ローカル（バックエンド / ネットワーク / アカウント / クラウド処理は一切なし）。

## 進め方の鉄則
- 各Stageは**最小で動くもの**を作る。宋其が実機確認してから次に進む。
- **勝手に先のStageを実装しない。**
- 触覚・音・"画"は実機でしか正解が出ない。シミュレータで止めない。
- 実機ビルドが通ることを各Stageのゴールにする（`xcodebuild` でエラーを潰してから引き渡す）。

## 設計原則（破ったら失敗・詳細は仕様書 §1）
1. SNS機能を一切載せない（フィード/フォロー/招待/共有先の内蔵すべて禁止）。
2. 完全ローカル完結（サーバー/アカウント/クラウドなし）。
3. 差別化は"出力の画"と"操作の身体感覚"の2点のみ。
4. フィルムの"画"は借りない。CCD/Y2Kに振り切る（"音"は写ルンです系を借りてよい）。
5. 待ちを課金で飛ばさない（課金は画質/フォーマット/透かし除去のみ）。

## 触覚の絶対ルール（仕様書 §3）
- 連写は `.hapticTransient` の高速連打で作る。**`.hapticContinuous`（持続振動）を連写に使わない。**
- `.hapticContinuous` を使ってよいのは**起動シーケンスのみ**。
- `CHHapticEngine.capabilitiesForHardware().supportsHaptics` で対応判定。非対応機は無音フォールバック。

## 現在地
- Stage 0 未着手（雛形＋カメラプレビュー）。
