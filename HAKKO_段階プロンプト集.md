# HAKKO — Claude Code 段階プロンプト集

使い方: Claude Codeを起動したプロジェクトで、`HAKKO_仕様書.md` を同じフォルダに置き、下のStageを**上から順に**貼る。各Stageは実機ビルド確認してから次へ。触覚・音は実機でしか判断できないので、シミュレータで止めない。

**前提の共有プロンプト（最初に1回貼る）:**
```
このプロジェクトは iOS アプリ「HAKKO」。同フォルダの HAKKO_仕様書.md が唯一の仕様。
実装前に必ず仕様書の「1. 設計原則」を読み、それに反する提案はしないこと。
完全ローカル（バックエンド/ネットワーク/アカウントなし）。Swift + SwiftUI + AVFoundation + Core Haptics + Core Image。
各Stageは最小で動くものを作り、私が実機確認してから次に進む。勝手に先のStageを実装しない。
```

---

## Stage 0 — 雛形＋カメラプレビュー
```
Stage 0 を実装。
- SwiftUI の単一画面アプリ雛形。
- カメラ権限（NSCameraUsageDescription）とフォトライブラリ追加権限（NSPhotoLibraryAddUsageDescription）を Info.plist に。
- AVCaptureSession で背面カメラのライブプレビューを全画面表示（AVCaptureVideoPreviewLayer を UIViewRepresentable でブリッジ）。
- 画面下に丸いシャッターボタン（まだ機能なし）。
- Core Haptics 対応判定（supportsHaptics）を起動時にログ出力。
ビルドが通り、実機でプレビューが映る状態をゴールに。
```

## Stage 1 — 強制フラッシュ連写バースト＋触覚の"食感"
```
Stage 1 を実装。仕様書 §3 の触覚ルールを厳守。
- AVCapturePhotoOutput で写真撮影。flashMode = .on（強制フラッシュ）。
- シャッターボタン長押しで連写バースト。最大枚数の上限あり（定数 maxBurst = 8、後で変更しやすく）。
- 【重要】連写の各撮影発火に同期して Core Haptics の .hapticTransient を1発ずつ鳴らす。
  .hapticContinuous は絶対に使わない（持続振動は禁止／仕様書 §3）。sharpness/intensity は定数で切り出し、私が調整できるように。
- 連写速度は触覚が団子にならない範囲に制限（撮影間隔の下限を設ける）。
- 撮った枚数をメモリ上の配列に保持（まだ加工/保存なし）。
ゴール: 実機で長押し連写中、1枚ごとに指へ離散的な"ガシャ"が返る食感を確認できる。
```

## Stage 2 — CCD風"発光"処理パイプライン
```
Stage 2 を実装。仕様書 §5。
- Core Image で調整可能な処理チェーンを作る。各効果は強度パラメータ（0.0–1.0）で外から制御可能に：
  1) ハイライトのブルーム/白飛び
  2) パープルフリンジ/色収差（明るいエッジに色滲み）
  3) ハイライト付近の色転び（マゼンタ/シアン被り）
  4) 粒状ノイズ
- パラメータを1つの struct（CCDRecipe）にまとめ、初期プリセットを1つ用意。
- 撮影した各バースト画像にこの処理を適用してプレビュー表示。
- 重くて Core Image で厳しい効果があれば、その効果だけ Metal(CIKernel) 化を提案（勝手に全部Metalにしない）。
ゴール: 実機で連写した画に"美しい破綻"が乗り、CCDRecipe の値変更で見た目が変わる。値の追い込みは私がやる。
```

## Stage 3 — 発光チャージ体験（写ルンです系）
```
Stage 3 を実装。仕様書 §3・§4。
- 撃ち切り（上限到達 or 指を離す）後、発光チャージ状態に入り 3.5秒間 再発光をロック。
- チャージ音を AVAudioEngine で合成生成（プリレンダ音源ではなくリアルタイム合成）：
  低→高へのピッチスイープ（写ルンです昇圧音風）＋軽い歪み/ノイズ、最後は高周波で高止まり。3.5秒。
- チャージ中は発光ゲージ（空→満タン）を画面に表示。数字は出さず、ゲージと音で"溜まり"を見せる。
- チャージ完了の瞬間に .hapticTransient を1発（"撃てる"合図）。
- サイレントモード時は音の代替として触覚を弱く添える。
- チャージ中にシャッターを押したら"不発演出"（短いカチッ音のみ、撮影しない）。
ゴール: 連写→撃ち切り→スイープ音で溜まる→完了の触覚→また撃てる、が実機で気持ちよく回る。秒数と音は私が調整。
```

## Stage 4 — 起動シーケンス
```
Stage 4 を実装。仕様書 §2・§3。
- アプリ起動時、撮影可能になるまでに短い起動シーケンス（約1.5–2秒）：
  レンズ繰り出し風のモーター音（AVAudioEngineで合成）＋ Core Haptics の .hapticContinuous で持続振動を音に同期。
  ※持続振動を使ってよいのは起動のみ（連写では禁止）。
- 液晶が点くような簡単な画面演出（暗→プレビュー点灯）。
- 初回は full シーケンス、2回目以降はスキップ可能な設定フラグを用意（UIはまだ簡易でよい）。
ゴール: 実機で"機械が起きる"数秒を音＋振動で体感できる。
```

## Stage 5 — 書き出し／透かし／買い切りの器
```
Stage 5 を実装。仕様書 §7。
- 処理済み画像を Photos に書き出し。無料は透かし付き（右下に控えめなHAKKO意匠、後で差し替え可能なPNGレイヤーとして）。
- StoreKit 2 で買い切りアンロック（product 1つ）。購入済みなら透かし除去＋高解像度書き出しを解放。
- 【禁止】クールタイム短縮・連写枚数UPを課金要素にしない（仕様書 §1-5）。
- CCDRecipe を複数持てる構造にし、「今月のレシピ」を1つ選べるプレースホルダを用意（月替わりの器。中身の切替ロジックは後日）。
ゴール: 撮影→加工→透かし付き書き出しが無料で回り、買い切りで透かし除去が解放される。
```

---

## Claude Code に任せない部分（宋其の担当）
1. **CCDRecipe の数値追い込み** — "美しい破綻"の正解は実機の目でしか決まらない。
2. **チャージ音の最終質感** — 合成パラメータの微調整（あなたの音響感覚）。
3. **連写触覚の手触り** — intensity/sharpness と連写速度の綱引きを実機で。
4. **透かしの意匠** — 世界観を壊さないデザイン。
5. **月替わりレシピの中身** — プリセットの創作。

## 別トラック（アプリ実装と並行）
- Apple Developer 登録（$99/年）— 実機の触覚チューニングに必須。Stage 1 の前に。
- J-PlatPat 商標検索（9類・42類）— 名称「HAKKO/発光」。
- 起動前の最小検証（推奨）: Stage 3 のチャージ音だけ先に単体で試作し、"エモい"が来るか確認してから全体へ。
