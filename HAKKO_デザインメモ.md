# HAKKO デザインメモ（仕様書§5の補足）
※ これは創作方向の生きたメモ。唯一の仕様は HAKKO_仕様書.md。

## 0. 大原則
- CCDらしさはセンサー単体でなく「当時のカメラシステム全体」の産物。単一の色処理(青い/派手/粒状)に集約しない。
- HAKKOの目的は正確な再現でなく「破綻の増幅」(仕様書§4-5)。以下は"説得力のある壊し方の材料"として使い、値は誇張方向へ振る。値の追い込みは宋其の創作領域。
- 機種×ISO×光源等の完全プロファイル化はpost-MVP。MVPは調整可能パラメータ群で十分(仕様書§10)。

## 1. 参考機種（作例リファレンス用。実装で名称/ロゴ/外装/UIは使わない）
- Panasonic DMC-F1: 2002年, 1/2.5型CCD 320万画素, 35-105mm相当 F2.8-4.9, ISO50-400
- Canon IXY DIGITAL 10: 2007年, 1/2.5型CCD 710万画素, 35-105mm相当, ISO80-1600
- Panasonic DMC-FX30: 2007年, 1/2.5型CCD 720万画素, 28-100mm相当, ISO100-1250
- MVP初期は実名を出さず「2002年3MP/35mm」「2007年7MP/35mm」「2007年7MP/28mm」の3プロファイルで抽象化。
- 現状のライブプレビューはiPhoneの計算写真で「画質が良すぎる」。CCDは逆に劣化方向で作る（これは想定通り。Stage 2で壊す）。

## 2. 画の特徴（Stage 2 CCDRecipeで再現）
- 被写界深度が深い: ポートレートぼけを足さない。むしろ35mm相当へクロップ＋出力3-7MP制限で近づける。
- ハイライトが急に飛ぶ: HDR的な明暗両立をしない。飽和直前だけ軽く圧縮→短い肩で白へ。単純ハードクリップは避ける。
- ハイライトの色転び: マゼンタ/シアン/微グリーン寄り(CCD特有)。
- ノイズは信号依存: 暗部ほど強く明部ほど弱く。ISOで色ノイズとNRの比率を変える。最後に均一に乗せない。
- 輪郭ハロ: 当時の過剰シャープによる縁取り。
- CCDスミア/ブルーム: 常時でなく点光源・直射・強反射・露出オーバー時のみ。静止画は必ずしも出ない。
- フラッシュが見た目の大部分を決める: 正面発光/手前だけ明るい/背景急に暗い/硬い影/色温度ズレ。最低「フラッシュなし」「正面フラッシュ」の2プロファイル。

## 3. 処理順序（Stage 2のチェーン設計）
レンズ(ボケ/色収差) → センサー飽和 → 信号依存ノイズ → デモザイク → WB/色変換 → NR → トーンカーブ → 輪郭強調 → 縮小 → JPEG圧縮

## 4. 撮影設計（Stage 1-2）
- 物理カメラを明示指定し、仮想デバイス自動融合(isAutoVirtualDeviceFusionEnabled)を無効化。自動レンズ切替も避ける。※Stage 1で軽く仕込める範囲は仕込む。
- 対応端末はBayer RAW優先(availableRawPhotoPixelFormatTypesを毎回確認)。ProRAWは合成が入るのでCCD素材には通常RAWが制御しやすい。→post-MVP/買い切り側でも可。
- 露出/WBを固定しすぎない。決定論的に(同条件なら同結果)少しだけ外す。

## 5. UIの方向性
- 現状のフラットUIは仮。要リデザイン。
- 方向: カメラ本体のスキューモーフィズムではなく、当時のY2K/Frutiger Aero的UIの「リッチさ・マテリアル感(光沢/半透明/クローム/アクア/グラデ/液晶セグメント感)」をリサーチし、現代のディスプレイ・操作系で再解釈するネオY2K。
- 機能軸(Stage 0-5)とは別のデザイン軸。コアループが回ってからStage 2以降と並走で詰める。

## 6. 法務・プライバシー（仕様書§1と一致）
- 「本物のCCDで撮影」等の表現やEXIFへの実機名偽装をしない。CCDコンパクトの撮影特性を模したシミュレーションと明示。
- メーカー名/ロゴ/カメラ外装/UIを模倣しない。UIは独自設計(ネオY2K)。実機名は研究資料の範囲に留める。
- 写真は外部送信しない(完全ローカル)。フォトライブラリはaddOnly権限。

## 7. 未解決メモ
- Stage 0時点でライブプレビューの反応がやや遅い。Stage 1で撮影パイプラインを組む際にプレビューのプリセット/接続を見直して改善する。
- ライブプレビューにもCCD処理をかけるか未決定(没入感↑だが負荷↑=プレビュー反応と綱引き)。実機で判断。

## 8. フラッシュ連写のフィーリング検証（Stage 1・実機A/B）
当時のCCDコンパクトはフラッシュ連写を構造的に持たない。コンデンサへの再充電に時間がかかり、1発ごとに"チャージ"を待つのが物理制約だった(この待ち＝HAKKOの発光チャージ/Stage3の原体験)。一方でiPhoneは強フラッシュを高速に反復できてしまうため、そのまま連写すると「CCD機では起こり得ない毎枚発光の連射」になり、狙う身体感覚とズレる恐れがある。そこでStage 1に3つのフラッシュ挙動を暫定UIで切替可能にし(触覚＝食感は全モード共通に固定して比較を汚さない)、どれがHAKKOらしいかを実機で決める: (1) flashEveryShot＝毎枚フラッシュを試み、光れる限界まで速度を自動で落とす(＝当時の再充電律速に近い)、(2) flashBurstOff＝フラッシュを切って速い連写のガシャガシャ食感を優先、(3) flashAsAvailable＝フラッシュONだが速度は落とさず光れる時だけ光る間引き許容(一眼＋ストロボ風)。決めたら不要なcaseを消して1モードに固定する。この検証結果はStage3の発光チャージ設計(再発光ロックの秒数・演出)に直結する。

### 8.1 実機計測で判明した事実（写真フラッシュ連写のボトルネック）
- 撮影発火→露出確定(meter)と露出確定→処理完了(capture)を分けて計測した結果、フラッシュONの連写が遅い主因は**プリフラッシュ測光**(meter=600〜1900ms)。保存(capture)は全モード50〜90msで速く無罪。
- **プリフラッシュ測光はAE/AWB/AFロックでも迂回できない**(flashLockedAEモードで検証・meterが下がらず)。写真フラッシュを使う限りこの数百ms〜秒の待ちは避けられない。
- flashMode=.offなら meter は一桁ms。ただし連写が途切れると**継続AF/AE再収束**で数百ms入る(全モード共通の底流)。これはロックで消せる。
- 結論: 「速い連写＋毎コマ発光」を写真フラッシュで両立するのは不可能。→ 発光を**トーチ(LED)の手動ストロボ**で作る方向へ(仕様書§5「発光は処理で作る」とも整合。トーチが本物フラッシュより暗くてもStage2のブルームで盛れる)。

### 8.2 トーチ手動ストロボ方式（有力候補）
- torchStrobeBurst: flashMode=.off(プリ測光回避)＋各発火でトーチを一瞬ポップ点灯(setTorchModeOn level:1.0→torchPopDuration後に消灯)。実機で meter=7〜11ms を維持でき、毎コマ発光ポップも視認可能。AF/AE/AWBロックを併用して「途切れ由来のつっかえ(再収束)」も抑える。
- steadyStrobeBurst: 固定インターバル(beatInterval)のメトロノーム駆動。felt層(触覚＋ポップ)を撮影完了とは無関係に等間隔で打ち、撮影が追いつかないビートは撮影だけスキップ。iOSの撮影完了の揺らぎ(0.5〜2.5s)を体感リズムから切り離す(仕様書§2 待ち=リズム)。※注意: AEロックはトーチ非点灯時の明るさで固定されるため、ポップ発光ぶんが露出に反映されず「ポップは光るが写真は暗い」になり得る。露出とポップ発光の同期はStage2のパイプライン設計で扱う。
- felt層の同期: 触覚を基準に揃える(仕様書§3 触覚が主役)。触覚プレイヤーを事前生成し、その直後にトーチポップを隣接させて「音より先に光る」逆転を抑える。
- 次善策(未実装): トーチのon/off切替が高速連写に追随しきれない場合、AVCaptureVideoDataOutputから連写フレームを抜く方式(テンポ完全均一・低画質=CCD向き)。パイプラインが変わるのでStage2以降で判断。

### 8.3 【確定】方針転換: 本物フラッシュ＋充電リズム 1本に統合（実験モード全破棄）
実機検証の結論。トーチ手動ストロボは **lockForConfigurationを撮影のたびに取ると撮影自体が壊れる(AVFoundation Code=-11830)** 構造限界に当たった。加えて重要な創作判断: スクリーンフラッシュ/後処理は「実際の光が被写体に当たらない=偽物」。ストロボの有無で照射範囲・falloff・背景の落ちが物理的に変わり、後処理で捏造できない。**ここがHAKKOの堀なので本物のフラッシュを使う。速さは捨て、フラッシュの実サイクルでリズムを組む**(疾走感でなく一発感／仕様書§2)。この回り道の価値=「速い連写＋発光」が構造的に不可能と計測で証明できたので、この結論に確信を持てる。
- flashMode=.on固定。トーチ/実験モード類は全削除。
- 速度を追わず、撮影完了を待って次を撃つ直列。maxBurst=5。コマ間の測光/充電待ちは"チャージ"として肯定(昇圧音はStage3で本格合成、今はプレースホルダ=空実装)。
- 触覚(.hapticTransient)を willCapturePhoto(=実発光の瞬間)デリゲートで鳴らして発光と厳密同期。「音より先に光る」を根本解決する狙い。

### 8.4 未対応TODO（コミット時点・後で対処）
2回のコードレビュー(reviewer)で出た未反映の指摘。この状態のまま一旦コミットした。
- **[要検討/ブロッキング] 触覚同期のasyncホップ**: willCapturePhotoForで `sessionQueue.async` して触覚を鳴らしているが、sessionQueueが撮影処理で詰まると触覚が数十〜数百ms遅延し、狙い(実発光と厳密同期)が崩れ得る。理想はデリゲートスレッドで同期発火だが、HapticEngineはsessionQueue直列化前提(stoppedHandlerとのburstPlayer/isRunningレース回避)なので、単純に直接呼ぶとデータレース再発。→ HapticEngine側のburstPlayerアクセスをos_unfair_lock等で保護してデリゲートスレッドから直接叩けるようにする再設計が要る。要実機確認(まず現状で"音より先に光る"が実際に直ったか見る。asyncホップの遅延が体感で問題になるか未検証)。
- **[軽微/計測] エラー枚でmeterログが不正値**: willCaptureが来ないエラー撮影(-11830等)では inFlightWillCaptureTime が前枚の値のまま残り、meter/totalが負値や巨大値になる。fireNextShotで inFlightWillCaptureTime=0 リセット＋didFinishで0なら分解せずtotalのみ出す分岐を入れる。デバッグログのみなので実害小。
- **[Stage3で対処] チャージ音の停止保証と冪等契約**: start/stopChargePlaceholderは今は空。Stage3で実音を載せる際、(1)stopは冪等必須(正常系でwillCapture＋didFinishの2回呼ばれる)、(2)isBursting=falseになる3経路(stop/stopBurst/撃ち切り)全てで停止保証、特にstop()はsession停止で飛行中didFinishが来ずチャージ音が鳴り残る恐れ→stop()にstopChargePlaceholder追加。
- **[実機確認項目] ジェスチャ**: 長押ししたまま指をボタン外へスライドして離すと onEnded が来ずstopBurstされないSwiftUI依存の挙動があり得る(maxBurstで自然停止はする)。実機で確認。
- **[任意] 非対応機検出**: supportedFlashModesにonが無い機種はフラッシュが焚かれない。configureSessionでsupportedFlashModesをログしておくと実機検証が速い。

## 9. 発光と連写の最終方針（Stage 1決着の総括）
- 発光は必ず物理の実光がシーンに当たること(照射範囲/falloff/背景の落ち/硬い影は後処理で捏造不可=偽物)。スクリーンフラッシュ/後処理だけの発光代替は却下。
- 連写の光源=連続トーチ(flashMode=.off/常時点灯/-11830回避)。撃ち切りのキメ1枚=本物キセノン(遅い1fps/検証済6.5秒再充電/ドラマチックな発光の画)。Stage2の処理で発光を増幅。
- 毎コマ本物フラッシュ連写は不可(プリ測光でAE/WB強制上書き・止められない/実機確認済/§8.1)。トーチ手動ストロボも不可(-11830=lock競合/実機確認済/§8.3)。
- コマ間の待ちは昇圧音(写ルンです系)で埋め、変動を充電時間として味にする。触覚はwillCapturePhoto(実発光の瞬間)で発火し光と同期。
- 凍結はシャッター速度で作る(トーチは連続光なので/§14ノイズ歓迎と相性◎)。→customLocked targetShutterで実装(§8.9続き)。

## 10. 撮っている感（CCDデジタルに接地）
- 手触りは電子的・合成音(機械一眼のモータードライブではない)。リーフシャッターは無音→スピーカーで合成シャッター音を鳴らしていた。起動ジングル/AF合焦ビープ/操作音/撮影後レビュー/アクセスランプが個性。
- 音=CCDデジタルに忠実に自作合成(録音は権利グレー/§3法務)。触覚=再現元が無い(当時機は無振動)ので設計された翻訳=ガラスの物理ボタン欠損を埋める、電子的で軽く鋭いクリック。充電キーン音はキセノンの現象でフィルム/CCD共通(仕様書§4借用OK)。

## 11. スタンス（反実仮想ヘリテージ）
- 「1999-2007のカメラ会社が今アプリを作ったら」。ハードはiPhone。旧メーカーからは思想だけ継ぐ(構造・デジタル制御・UI・アフォーダンス)。旧機の外装は真似ない。必要なものだけを旧メーカーの美意識で魅力的に見せる。声=無口・機械的・冷たい精密機械寄り。

## 12. 所作の原則
- すべての所作・演出は実在する機能か制約の解であること。装飾なら削る。偽の制約や飾りは作らない。実在する制約だけをドラマ化。
- keep: 起動シーケンス(初期化時間の解)/発光ゲージ/充電音/連写・撃ち切りの触覚。 足さない: スライドカバー起動(レンズ保護不要)。 保留: モードダイヤル(複数レシピ後)。

## 13. 操作系UI設計原則（旧コンデジUX→タッチ翻訳 ※知識ベースの設計推論=仕様書§5）
- 当時の論理: 主要動作1つ(シャッター)を最も押しやすい位置に。「撮る」ゾーン(人差し指)と「見る・設定」ゾーン(親指)の二分。ステータスは読み取り専用グリフで縁に。撮影後オートレビュー。液晶セグメント風タイポ。
- タッチ翻訳: 論理は盗むがエルゴノミクスは逆(片手スマホは下=親指→シャッターは下部・大・触覚)。ガラスは物理ボタンの指区別が無い→(1)視覚マテリアル差(2)触覚署名差(3)空間の一貫性で代替。
- 新言語=「発光するUI」: 旧機外装の丸コピー禁。各社のマテリアル感性だけ抽出(Canonの冷たいステンレス平面/Sonyのミニマル板/液晶セグメント/Frutiger Aeroの光沢半透明)。偽カメラ本体でなく精密な装置の表面。アクティブ要素は光を放つ(ゲージは光で満ち・シャッターは押下発光・起動は液晶点灯)=一般ネオY2Kとの差別化。

## 13.1 検証済み定量リファレンス
- 1/2.5型CCD=5.75×4.32mm・対角7.19mm・クロップ6.02倍。1/1.8型=7.11×5.33mm・4.87倍。標準画角35-39mm相当→iPhoneメインを約1.4-1.5倍クロップ・出力3-7MP。当時機は日中機敏だがフラッシュだけ遅い(6.5秒再充電)。

## 14. Stage 2 CCDルック設計図（リサーチ確定）
※発光・連写・撮っている感・スタンス・所作・UI・定量refの総括は §9〜§13.1 に集約。本章はStage2の画づくり詳細。

### 大結論: CCDルックは2バケツに分かれる
- 🟢収斂進化(物理再現でタダで出る/iPhoneでも): ①マゼンタ・ハイライト ②狭DR・硬い白飛び(iPhoneの計算処理を切る)
- 🔴偽装が必要(iPhone構造的に不可・能動的に描く): ③ブルーミング ④縦スミア ⑤小センサーのノイズ

### 最重要レバー: マゼンタ・ハイライト(検証3-0/darktable・RawPedia一次情報)
- 機序: WB色別ゲイン(R≈2.0/G=1.0/B≈1.4)が飽和チャンネルに作用。緑が最小ゲイン=最初にクリップ→生き残ったR,Bが緑の天井を超える→白がマゼンタに転ぶ。ハイライト補正をしなければ自然に出る(センサー物理ですらない)。
- レシピ: 線形光でR×2.0/G×1.0/B×1.4→緑が先にクリップ→補正しない(clamp/inpaintしない)→白飛び際がマゼンタ。
- ダイヤル: クリップ閾値(下げるほど早く広く)/ゲイン比(緑先→マゼンタ、青/赤先→緑/シアン)。色はマゼンタ-緑軸(WB tint)に乗る。
- HAKKOはフラッシュ機=ハイライト多発→この破綻が主役級。

### 機序(検証済):
- ブルーミング=フルウェル飽和で電荷が隣接(縦優先)に溢れる。小画素ほど悪化。iPhoneは対策済で出ない→描く。
- 縦スミア=インターラインCCDの遮光VCCDが強い点光源で漏れる別現象(ブルーミングと別/点光源・露出オーバー時のみ)。CMOSにVCCD無し→描く。
- 小画素(1/2.5-1/1.8型,約1.7-2.5µm,5-12MP)=小フルウェル→狭DR(約7-9EV)・スミア/ブルーム顕著。これが"美しい破綻"の根。

### Stage2パイプライン順序:
計算処理オフ→飽和/硬い白飛び→マゼンタ(WB×非補正クリップ)→信号依存ノイズ→ブルーミング(縦優先)→縦スミア(点光源時のみ)→輪郭ハロ/色収差/周辺減光→縮小/JPEG風。

### 未確証(=宋其の創作領域/仕様書§5): 具体DR段数・CFA/カラーマトリクス(Fuji/Kodakの色)・光学欠陥の実数・JPEG/シャープの実数。研究は機序の骨、味は実機で。
出典: darktable highlight-reconstruction, rawpedia White_Balance, adimec blooming-smear, IEEE 1156370。

### 8.5 【確定】発光=torch常時点灯・操作=自動連射（実機A/B決着）
速度モードの実機A/B(SpeedMode 4種)で決着。
- **torch常時点灯＋flashMode=.off が圧勝**: meter=2〜5ms(実測)。写真フラッシュ.on(600〜1700ms)の100〜300倍速い。「Native感と変わらない遅さ」を解消。
- flash locked(=.on+AEロック)も遅い(590〜1546ms)=プリ測光はロックで消えない(確定)。
- **torch custom-exp(setExposureModeCustom)は-11800エラー(Code=-16800)で不安定→不採用**。露出は継続オートのまま、torchだけ点灯するtorch contが正解(シンプルが勝った)。
- 散発する50〜200msの跳ねは継続AF/AE再収束。→ torch cont に AE/WB/AF の.locked(既存値保持・customは使わない)を併用して抑える。
- 確定実装: torch常時点灯＋.off＋開始時1回だけlockForConfiguration(torch点灯+.lockedロック)＋撮影中は触らない(-11830回避)。ZSL/responsive/fastCapturePrioritizationも有効化(torch=.offなので効く)。実験モード(SpeedMode)は全削除。

### 8.6 操作モデル: 一発トリガ自動連射
- 指離しで連写を止める方式は連打/離し判定のバグ源(実機で-11830や計測交差が多発)。→ **1回押したら maxBurst まで自動連射**に変更。指は置きっぱなし(長押し)でもよいが、離しは連射に影響しない。
- maxBurstは初期5→12に増やす(5は短い/動画素材数も兼ねる)。

### 8.7 【将来・忘れないこと】連写を合体して動画化
- 撮った連写バーストを1本の動画(GIF or mp4は未定)に合体するのが最終形。連写は動画のフレーム素材。
- 現状は capturedImages 配列にメモリ保持しているだけ。Stage2でCCD処理→Stage後半で動画エンコード(AVAssetWriter等)。
- torch常時点灯の連続フレームは「動く被写体の連続」を撮れるので動画化と相性が良い。フレーム間隔(テンポ)が一定であるほど動画が滑らか=torch方式の速さ・一定性が効く。

### 8.8 ガタつき(テンポ揺らぎ)の安定化: AE/WB/AFロックは撤去
実機ログ分析で、torch contでも散発する50〜200msのmeterスパイクが「各バーストの1〜2枚目」に集中と判明。原因は teardownで .continuousAuto に戻し→次のstartBurstで再 .locked する **locked↔continuousAuto の往復**で、バースト境界に再収束時間が乗っていた。しかも .locked を入れても跳ねは残った(効果薄・副作用大)。
→ **AE/WB/AF の .locked を全撤去**。torch常時点灯下では継続オートのままで露出は安定し、被写体変化にも追従する(連写→動画化の前提で被写体固定は不適。§8.7)。デバイス設定はtorch点灯のみ。
- 発熱スパイク(meter=258〜267 msの群)対策で maxBurst 12→8 に減。torch連続点灯の時間を短くしてスロットルを避ける。isTorchActiveで点灯失敗も検出ログ。

### 8.9 【決着】ガタつきの真因=継続オートAEの再測光。custom-locked(露出固定)で解消
一次調査(AVFoundation/Apple公式フォーラム/実測記事)＋実機A/Bでガタつき問題が決着。
- **真因**: torch常時点灯は実際に明るさを変えるので継続オートAEが反応し再測光する。これが meter に数百ms(実測471〜535ms)の群スパイクとして乗る。「大きく動かしながら撮る」と画面輝度が変わり多発=実機体感と機序が一致。
- **対策=露出完全固定(setExposureModeCustom)**: 露出/ISO/WB/AFを.locked/customで固定するとAE再測光が起きず meter が2〜5ms一定に。実機で auto+ZSL(スパイク大量)vs custom-locked(スパイク激減・体感スムーズ)を確認して決着。
- **-11800(前回の失敗)の回避**: isExposureModeSupported(.custom)確認→exposureDuration/isoをactiveFormatのmin/max範囲にclamp(範囲外が主因)→lockForConfiguration内で設定→ZSLはcustomと排他なので無効化。この正しい手順で-11800は出なくなった。
- **トレードオフ**: custom-lockedは露出固定=明るさ追従しない(CCD/写ルンです的でむしろ狙いに合う)。ZSLは無効化される。
- **残課題**: custom-lockedでも稀に134〜361msの跳ね(AF再収束/1枚目の露出未確定)。→AFレンズ位置固定・custom確定のcompletion待ちで詰められる。ただし画のA/B判断手段(サムネ表示)を先に用意してから最終決定する。
- **err=-17281/-12710は無害と確定**(Apple DTS明言・iOS26系でログ露出)。実害なし。動画方式を将来足す時のみ単一AVCaptureSession共有を守る(複数セッションが同カメラを奪うとFigエラー/競合)。
- 実装: ExposureMode(autoZSL/customLocked)の2モードA/BをUI切替で残置。決定後に1本化。
- **[動画フレーム方式・再実装時の必須条件]** 写真連写 vs 動画フレーム抽出のA/Bを実際にやると決めたら、正規フロー(レビュー付き)でクリーンに再実装する(過去のローグsubagent製プロトは未信用ゆえ破棄済・救出しない)。その際: 動画フレームは実効シャッターが遅く(30fps≒1/30〜1/60)被写体ブレしやすいので、露出を速いシャッターにロックして凍らせる設定が必須(torch連続光では発光で動きを止められないため=キセノンと違い連続光は被写体を止めない)。
